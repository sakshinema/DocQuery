# Optional: enables LLM-powered structured extraction and grounded Q&A.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini

# SQLite location. On Render, mount the persistent disk at /var/data and keep this path.
DOCQUERY_DB_PATH=./data/docquery.db

# Upload guardrails.
MAX_UPLOAD_MB=10
